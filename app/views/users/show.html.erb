<section class="dashboard-main">
	  <h1> <%= current_user.first_name %>'s Dashboard </h1>
	
	  <%= button_to 'Log Out', logout_path, method: 'delete', class: "btn btn-primary mb1 bg-teal" %>
	  <h3>Account Details</h3>
	  <ul>
	    <li> <%= current_user.first_name  %> <%= current_user.last_name %> </li>
	    <li> <%= current_user.email%> </li>
	  </ul>
	  <section>
	    <h1>Bookmarked Segments</h1>
	  </section>

	<% if !facade.repos(1).nil? %>
	  <section class="github_section">
	    <h1>Github Repositories:</h1>
	      <ul>
	      <% facade.repos(5).each do |repo| %>
	        <li><%= link_to repo.name, repo.html_url %></li><br>
	      <% end %>
	      </ul>

	    <h1>Followers:</h1>
	      <ul>
	      <% facade.followers(5).each do |gh_user| %>
	        <li><%= link_to gh_user.login, gh_user.html_url %></li><br>
	      <% end %>
	      </ul>

	    <h1>Following:</h1>
	      <ul>
	      <% facade.following(5).each do |gh_user| %>
	        <li><%= link_to gh_user.login, gh_user.html_url %></li><br>
	      <% end %>
	      </ul>
	  </section>
	<% end %>	
	


		<%= link_to "Connect to Github",
  	"https://github.com/login/oauth/authorize?client_id=586cd4146e2b7b6b2621&scope=repo" %>
</section>
